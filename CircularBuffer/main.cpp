#include <stdio.h>
//#include "CircularBuffer.h"

int main(int argv, char** args) {
/*	CircularBuffer buffer{{'\0'}, -1, -1};
	ResponseStatus response = insert_char(&buffer, 'A');
	if (response != OK) {
		printf("ERROR after insert\n");
		return -1;
	}*/
	return 0;
}